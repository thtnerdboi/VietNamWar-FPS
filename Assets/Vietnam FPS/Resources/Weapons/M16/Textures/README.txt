Place the M16 texture maps (e.g. base color, normal, roughness) in this folder.
The Rifle component will attempt to apply them to the instantiated model based on
resource paths configured in the inspector or the default names defined in the
script.
